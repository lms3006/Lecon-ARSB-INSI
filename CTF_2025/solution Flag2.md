Recherche des fichier texte sur le système 

```
find / -type f -name "*.txt" 2>/dev/null
```
après l’exécution de commande , On le répertoire suivants:

```
/etc/alternatives/insi.txt
```
je vais ouvrir le fichier insI.txt

```
cat /etc/alternatives/insi.txt
```
resultat:

```
flag{insi_f1ag2_f1ndf1le}
```
