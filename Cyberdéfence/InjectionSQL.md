C'est une technique d'attaque utilisant **SQL**

Manipiler,injecter,insérer des requêtes SQL à partir d'un utilisateur non-sécurité.

On peut faire une injection SQL a partir d'un **environnement vulnérable** (ex: formulaire)

###### Type d'attaque SQL

- injection SQL classique (In-bande) : mapiasa resultat direct (UNION SELECT)
- Bland SQL injection : manao exploitation sans retour d'erreur ()
- Out off Band SQLite : manao exploitation @ alalan'ny DNS na http